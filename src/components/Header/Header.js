import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <header class='Header'>
            <div>Logo</div>
            <div>Menu</div>
        </header>
    );
};

export default Header;
